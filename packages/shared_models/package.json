{
  "name": "@annex/shared-models",
  "version": "0.1.0",
  "description": "Shared domain contracts for ANNEX (locales, DTOs, enums).",
  "type": "module",
  "main": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "format:check": "echo ok"
  },
  "devDependencies": {
    "typescript": "~5.7.0",
    "vitest": "^2.1.8"
  }
}

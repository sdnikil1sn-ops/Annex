{
  "name": "@annex/shared-utils",
  "version": "0.1.0",
  "description": "Framework-agnostic utilities shared across ANNEX apps.",
  "type": "module",
  "main": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "format:check": "echo ok"
  },
  "devDependencies": {
    "typescript": "~5.7.0",
    "vitest": "^2.1.8"
  }
}
